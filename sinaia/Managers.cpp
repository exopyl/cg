#include "Managers.h"
/*
Managers::Managers ()
{
	m_pParsers3DManager = nullptr;
	m_pRenderingEngine = nullptr;
}

Managers::~Managers ()
{
	if (m_pParsers3DManager)
		delete m_pParsers3DManager;
	m_pParsers3DManager = nullptr;

	if (m_pRenderingEngine)
		delete m_pRenderingEngine;
	m_pRenderingEngine = nullptr;
}

bool Managers::Init (void)
{
	m_pParsers3DManager = new Parsers3DManager ();
	CPLUGINS_3DFiles *p3dFiles = new CPLUGINS_3DFiles ();
	p3dFiles->Plugins_LoadAll (nullptr);
	m_pParsers3DManager->Set3DFilesPluginsManager (p3dFiles);

	m_pRenderingEngine = new CRenderingEngine ();

	return S_OK;
}
*/
